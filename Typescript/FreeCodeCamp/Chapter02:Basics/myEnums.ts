// Enums are used to restrict users options/choices:
enum SeatChoice {
    AISLE = "Aisle Seat",
    MIDDLE = "Middle Seat",
    WINDOW = "Window Seat",
};

const hcSeat = SeatChoice.AISLE;
console.log(hcSeat);

export { };
